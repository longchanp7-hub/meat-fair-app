# 2026-09-15 実装修正の再開チェックポイント

## 実際に完了した範囲

対象: `longchanp7-hub/meat-fair-app` のみ。寿司フェアアプリは操作していない。
参照した main: `b74ce84f32457020dcfa78362bd12262291dd5f1`。

- `app/app.js` のチェーン絞り込みをブランド名のみの表示に変更。
- ブランドカードの「開催中 N件／近日開始 N件」を、件数のない状態表示に変更。
- カード内フェア見出しの件数を削除。
- 開催中・新着・近日開始・終了間近の絞り込み、選択解除、内部集計は維持。
- 取得制限・一部情報要確認などの重要な注意書きはカード本文に残す。
- `tests/fair-count-display.test.mjs` を追加。

PWA、サービスワーカー、更新処理、取得済みデータ、ギャラリーのコードは変更していない。
このコミットは全15チェーン修正の完成版ではない。公開版へのマージ・デプロイは行っていない。

## 実行した検証

GitHub連携で取得した `app/app.js` をローカルに再現し、変更前のGit blob SHAが
`d060beebff6d4c83b35d6a14ccd3d4a2d83adfdb` に一致することを確認した。

- Node.js v22.16.0 で構文確認: 成功。
- 追加した6項目のテスト: 変更前は3成功・3失敗、変更後は6成功・0失敗。
- テストは実際の描画関数を使い、DOM・ギャラリー・状態判定の外部依存をスタブ化した限定的な回帰テスト。
- 既存のテスト全体、実ブラウザーでのレイアウト、実際のPWA動作、公開版の反映は未検証。

再実行:

```sh
node --input-type=module --check < app/app.js
node --test tests/fair-count-display.test.mjs
```

## 未完了: 引き続き必要な仕様

以下は会話で確認できる要件。実装済みとして扱わない。

### 全15チェーン共通

- 期間限定フェアだけでなく、通常の食べ放題コース・税込料金を読みやすいタイルで表示。
- 上位コースにしかない目玉メニューを、公式の訴求・コース差を根拠として表示。実在する対象コースと提供条件を確認する。
- 公式に飲み放題がある場合は必ずタイルを表示。アルコール／ソフトドリンク、料金、時間、ラストオーダー、追加注文条件、対象コース、店舗・曜日条件は確認できる範囲で示す。未確認の金額や条件を作らない。
- 予約限定・会員限定など、価値のあるキャンペーンと適用条件を表示。
- フェア、注目メニュー、通常コース・料金、飲み放題をデータ上も区別する。通常メニューを期間限定フェアとして扱わない。
- フェア→注目メニュー→通常コース・料金→飲み放題→条件付きキャンペーン→公式リンク、という構成を基本とする。
- 画像の意味、比率、画面幅を基準にギャラリーを自動配置し、ブランド別の固定CSSを増やさない。公式画像の読みやすさと価格の可読性を維持。
- PWAを保護し、元の寿司アプリには変更しない。

### この会話で追加された4チェーン

- 露菴: 掲載中フェアは維持し、見えていない通常食べ放題の料金タイルを追加。
- 肉匠坂井: フェアとは別に通常食べ放題コース・料金タイルを追加。「台湾グルメフェアが中心」という会話上の情報は、実装時に公式情報で再確認する。
- 熟成焼肉いちばん: 右の小タイルが下がり黒い空白が出る配置を修正。空き領域を通常コース・料金や適切な注目メニューで構成。会話中の料理名やコース数を未検証で確定しない。
- 串家物語: 通常コース・料金、注目メニュー、飲み放題を追加。ユーザー提示の「9月1日開始の串の日キャンペーン／ディナー食べ放題20%引き／ネット予約限定」は公式に期間・対象店・条件を確認してから掲載。ネット予約限定の条件を目立たせる。

以前の11チェーンの個別修正案は、このチェックポイント作成時に原文を全件再取得していない。
既存の修正計画・会話から取得し、上記の新しい共通ルールと照合する必要がある。
「全15社の要件を漏れなく実装済み」と報告してはならない。

## 停止原因と実行状態の区別

以前の音声・テキスト切替時に実行が止まった原因は特定できていない。
本実行ではGitHub連携による読み取りに成功したが、コンテナからの直接cloneはDNS解決エラーだった。
後者を以前の停止原因と断定しない。
この記録はバックグラウンド実行や自動再開を開始するものではない。
