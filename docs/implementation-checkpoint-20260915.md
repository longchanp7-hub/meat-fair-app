# 実装チェックポイント — 2026-09-15 JST

## 実行状態

対象: longchanp7-hub/meat-fair-app。既存の寿司アプリは変更禁止。
基準コミット: b74ce84f32457020dcfa78362bd12262291dd5f1。
今回の保存範囲は件数表示の削除、回帰テスト、確定指示の記録のみ。全15社の追加タイル実装完了を意味しない。mainと公開版へはまだ反映していない。このファイルの存在は作業がバックグラウンドで継続していることを意味しない。

## この変更で実施したこと

- チェーン絞り込みの「開催中 N件」「近日 N件」と補助行を削除し、チェーン名のみ表示。
- ブランドカードの件数表示も削除。開催中・近日開始という状態、個別フェア情報は維持。
- 開催中・新着・近日開始・終了間近の4タブ、絞り込みと解除、取得失敗警告、近隣店舗案内を維持。
- countForは空欄時の近日開始タブ案内に使用しているため内部関数として維持。
- 初期データ取得と自動更新のコードは基準版と同一。manifest、service worker、PWA導入処理、Actions、収集データは変更していない。

検証: Node.js v22.16.0で構文確認成功。node --test tests/brand-counts.test.mjs は8件成功、失敗0件。テストは状態判定とギャラリーをスタブ化した単体テストであり、実ブラウザの見た目、全収集処理、PWAの実機動作を検証したものではない。

## 会話本文で確認できた、残りの確定指示

### 全15チェーン共通

1. 期間限定フェア、上位コース限定の注目メニュー、通常食べ放題コースと料金、飲み放題、価値のある予約・会員限定キャンペーン、公式サイトを分かるように整理する。
2. 上位コース限定の目玉メニューは公式の訴求とコース差を根拠に選定。骨付きカルビ・厚切り牛タン等はユーザーの例であり、個別チェーンで提供中と未確認のまま断定しない。公式画像が確認できれば画像付きタイルにする。
3. 飲み放題が公式に確認できる場合は必ずタイル表示。アルコール／ソフトドリンク、税込価格、制限時間、ラストオーダー、単品／コース追加、対象コース、曜日・店舗条件を確認できる範囲で示す。未確認は推測しない。
4. 通常コース、料金、注目メニュー、飲み放題は期間限定フェアとは別データで扱う。画面上のフェア件数は表示しない。
5. 画像の意味・縦横比・画面幅に応じた自動配置にする。ブランド固有の固定CSS追加を避け、余白、画像切れ、文字の読みにくさを防ぐ。
6. 寿司アプリを変更しない。新アプリの既存PWA、データ更新、公開検証を保護する。

### 今回追加された4社

- 露菴: フェアに加えて、見やすい食べ放題コース・料金タイルを追加。
- 肉匠坂井: 期間限定フェアとは別枠に通常食べ放題コースと料金を追加。会話中の台湾グルメフェアが現時点の唯一のフェアかは別途公式確認。
- 熟成焼肉いちばん: 右側の小タイルが下がり下部が黒い空白になる問題を、画像配置と料金・注目メニュータイルの組み合わせで解消。実際の画像・コース数は最新公式情報で確認。
- 串家物語: 料金、注目メニュー、飲み放題に加え、価値のある予約限定キャンペーンを掲載。ユーザー提示の「9月1日からの串の日キャンペーン、ディナー食べ放題20%引き」は掲載候補であり、期間・対象店舗・割引条件を公式確認してから登録。ネット予約限定を明示。

対象15社: 焼肉きんぐ、牛角、しゃぶ葉、ゆず庵、和食さと、あみやき亭／Plus、しゃぶしゃぶ温野菜、露菴、カルビ大将、すたみな太郎、ステーキのあさくま、肉匠坂井、熟成焼肉いちばん、安楽亭、串家物語。

## 未完了・次の開始点

- 上記4社以外の11社について、過去に回収済みと説明された個別指示の全文は、この実行では未取得。一般方針で埋めて「全件反映」としない。以前の計画書・会話本文と照合する。
- 全社の通常料金・上位コースの目玉・飲み放題・対象キャンペーンの公式確認、データ構造分離、収集・表示ロジック、画像配置修正。
- スマートフォン／Fold幅での画面比較、既存テスト全体、PWA・更新処理の検証。
- mainへの統合、公開、公開後検証。公開時には既存のキャッシュ更新方針を確認する。
- 再開時はこのコミットと最新mainの差分を先に確認し、他の作業を上書きしない。

## 停止について確認できた範囲

GitHubコネクターの読み取りは成功。ローカルからのgit cloneはDNS解決エラーで失敗したため、GitHubコネクターで基準版app.jsを取得した。元ファイルのGit blob SHAがd060beebff6d4c83b35d6a14ccd3d4a2d83adfdbと一致することを確認してから編集した。このDNSエラーを、ユーザーのChatGPTアプリで音声からテキストへ切り替えた際の停止原因と混同しない。ChatGPT側の停止原因や他スレッドの実行状態は断定できない。
