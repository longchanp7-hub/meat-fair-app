{
  "name": "meat-fair-app",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "update": "node scripts/update-fairs.mjs && node scripts/update-gallery.mjs",
    "update:gallery": "node scripts/update-gallery.mjs",
    "quality-gate": "node scripts/quality-gate.mjs --self-test",
    "verify": "node scripts/verify-data.mjs && node scripts/verify-gallery.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
